# AESCmd
A simple command-line application written in Java that allows users to securely encrypt and decrypt text using the AES (Advanced Encryption Standard) algorithm with a 128-bit key. Includes input validation and exception handling for smooth user experience.
